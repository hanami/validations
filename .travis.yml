language: ruby
sudo: false
cache: bundler
script: 'bundle exec rake test:coverage --trace'
rvm:
  - 2.2
  - 2.3
  - ruby-head
  - jruby-head
  - jruby-9000

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
